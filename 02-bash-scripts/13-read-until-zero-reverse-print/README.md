# Read Until Zero, Reverse Print

## What it does
Repeatedly prompts for a number and stores it in an array until the user enters `0`. Then prints all the collected numbers in **reverse order**, each prefixed with its (0-based) index.

## Usage
```bash
chmod +x read_until_zero_reverse.sh
./read_until_zero_reverse.sh
```
