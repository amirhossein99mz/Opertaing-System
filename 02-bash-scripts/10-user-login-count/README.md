# User Login Count

## What it does
Prompts for a username and prints how many active login sessions that user currently has, using `who`.

## Usage
```bash
chmod +x user_login_count.sh
./user_login_count.sh
```
